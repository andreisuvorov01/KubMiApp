# Copy this file to keystore.properties and keep it outside version control.
# Environment variables with the same names can be used instead.
KUBMI_RELEASE_STORE_FILE=app/kubmi-release.jks
KUBMI_RELEASE_STORE_PASSWORD=change-me
KUBMI_RELEASE_KEY_ALIAS=kubmi
KUBMI_RELEASE_KEY_PASSWORD=change-me
