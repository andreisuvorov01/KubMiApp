package com.example.kubmi.domain.model

/**
 * Raw entry from the panel index pages: keep title exactly as on the page + resolved URL.
 */
data class ScheduleIndexEntry(
    val title: String,
    val url: String
)


